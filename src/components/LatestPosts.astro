---
    const posts = [
      {
        title: 'Typography in Astro Blog',
        link: '/posts/post-1',
        excerpt: 'This post demonstrates various typography elements that you can style using Tailwind CSS.',
        image: 'https://placehold.co/600x400',
      },
      {
        title: 'Advanced Typography',
        link: '/posts/post-2',
        excerpt: 'This post explores more advanced typography elements and how to style them.',
        image: 'https://placehold.co/600x400',
      },
    ];
    ---

    <div class="latest-posts">
      <h2 class="text-xl font-bold mb-4">Latest Posts</h2>
      <ul class="grid grid-cols-1 md:grid-cols-2 gap-4">
        {posts.map((post) => (
          <li key={post.link} class="bg-white rounded-lg shadow-md overflow-hidden">
            <img src={post.image} alt={post.title} class="w-full h-48 object-cover" />
            <div class="p-4">
              <a href={post.link} class="text-lg font-semibold text-blue-600 hover:underline">
                {post.title}
              </a>
              <p class="mt-2 text-gray-600">{post.excerpt}</p>
              <a href={post.link} class="mt-2 inline-block bg-blue-600 text-white px-4 py-2 rounded hover:bg-blue-700">Read More</a>
            </div>
          </li>
        ))}
      </ul>
    </div>
